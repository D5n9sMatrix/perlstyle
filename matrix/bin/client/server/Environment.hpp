/**
 * (In perhaps more familiar notation, equation (2.53) is just V(x) =
 * xx) 2 /(n − 1).) From equation (2.45), we see that
 */

#ifdef Enviroment
#define enviroment(file, mode, source, env = TRUE, name, ...)
#define file(description, mode, source)
#define mode(...)
#define env = TRUE
#define name(list(...))
#endif // Environment

