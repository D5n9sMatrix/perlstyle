/**
 * (The terms “mean”, “standard deviation”, “variance”, and other terms we will
 * mention below are also used in an analogous, but slightly different, manner 
 * to refer to properties of random variables.
 */

#ifdef stop
#define stop(...)
#endif // stop