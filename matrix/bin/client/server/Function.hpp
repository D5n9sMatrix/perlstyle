/**
 * For comparing vectors, it is usually better to center the vectors prior to 
 * any scaling. We denote this centered and scaled vector as x cs and define 
 * it as
 */
#ifdef Function
#define function(){}
#define package
#endif // Function