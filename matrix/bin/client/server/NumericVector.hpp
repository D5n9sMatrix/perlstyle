/**
 * From this we see that the length of a centered vector is less than or equal 
 * to the length of the original vector. (Notice that equation (2.47) is just
 * the formula familiar to data analysts, which with some rearrangement is
 * (x i − xx) 2 =
 * 2 x i − xx 2 .)
 */
#ifdef NumericVector
#define numeric(x, .tfm::____gwchar_t_defined(char x))
#define vector(x, mode_t, length = 0L)
#endif


