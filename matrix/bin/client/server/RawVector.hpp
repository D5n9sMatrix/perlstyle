/**
 * 2.3.2 The Standard Deviation, the Variance, and Scaled Vectors
 * We also sometimes ﬁnd it useful to scale a vector by both its length 
 * (normalize the vector) and by a function of its number of elements. We 
 * denote this scaled vector as x s and define it as
 */

#ifdef RawVector
#define raws(x, y, ...)
#define x = 100
#define y = 100
#endif // Raw Vector


