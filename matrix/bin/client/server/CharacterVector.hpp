/**
 * This is indeed the case. The angle between two
 * vectors, for example, is not invariant to centering; that is, in general,
 * angle(x c , y c ) = angle(x, y)
 * (see Exercise 2.15).
 * (2.49)
 */

#ifdef CharacterVector
#define strings(x, y = NULL, ...)
#define x = 10
#define y = NULL
#define ...
#endif // Character Vector
