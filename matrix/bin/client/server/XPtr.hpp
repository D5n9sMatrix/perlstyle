/**
 * The angle between two vectors is not changed
 * by scaling (but, of course, it may be changed by centering).
 * The multiplicative inverse of the scaling factor,
 */
#ifdef XPtr
#define external_pointer(x, y, ptr = "language")
#define x = 100
#define y = 100
#define ptr = "language"
#endif // X Ptr

