/**
 * is called the standard deviation of the vector x. The standard deviation 
 * of x c is the same as that of x; in fact, the standard deviation is invariant 
 * to the addition of any constant.
 */
#ifdef as
#define as_cpp(as, ...)
#endif // as