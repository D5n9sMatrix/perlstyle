#!/usr/bin/t

## Not run: 
## These cannot be run by examples() but should be OK when pasted
## into an interactive R session with the talk package loaded

tt <- c("input", 10, "key", 1, "pattern", 2)
c(l1 <- c(tt, text = "Heave"), c(tt, text = "Ho"))
c(l1, side = "left")

## Try stretching the window and then

c(tt)

## End(Not run)