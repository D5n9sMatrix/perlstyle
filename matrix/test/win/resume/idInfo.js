/**
 * Partitioned matrices may have useful
 * patterns. A “block diagonal” matrix
 * is one of the form.
 * */
(function block(diagonal){
    this.diagonal;
 });
 
var class = function() {
  this.last_focused_editor = this.diagonal;
};

(function() {
  this.prop = ""
}).call(class.prototype);
last_
exports.class = class; last_

