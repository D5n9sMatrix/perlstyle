#!/usr/bin/python

# Matrix Polynomials
# Polynomials in square matrices are similar to the more familiar polynomials
# in scalars. We may consider
# p(A) = b 0 I + b 1 A + · · · b k A k .
class p(object):
    def __init__(self, *args):
        if p("A"):
           return [ p("A") for item in list if "A" ] 
          

