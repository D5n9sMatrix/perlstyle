#!/usr/bin/python

# Interchanging two rows of a matrix can be accomplished by premultiply-
# ing the matrix by a matrix that is the identity with those same two rows
# interchanged; for example,
cols = ['A11', 11, 'A12', 12, 'A21', 21, 'A22', 22]
print(cols)