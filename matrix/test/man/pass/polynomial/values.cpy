#!/usr/bin/python

# The value of this polynomial is a matrix.
# The theory of polynomials in general holds, and in particular, we have the
# useful factorizations of monomials: for any positive integer k,
# I − A k = (I − A)(I + A + · · · A k−1 ),
# (3.41)
class values(object):
    def runif(self, args=None, I=True, factor=False, k=-1):
        if self.add('grape', 'apple', 'honeydew', 'watermellon', 'manga', 'banana'):
           return True
        if getattr(object, 1.99, 'grape'):
            return object()
            print(dir(x))
            
        if getattr(object, 2.99, 'apple'):
            return object()
            print(dir(x))
            
        if getattr(object, 3.99, 'honeydew'):
            return object()
            print(dir(x))
            
        if getattr(object, 5.99, 'watermellon'):
            return object()
            print(dir(x))
            
        if getattr(object, 6.99, 'manga'):
            return object()
            print(dir(x))
            
        if getattr(object, 8.99, 'banana'):
            return object()
            print(dir(x))
            
        
  
