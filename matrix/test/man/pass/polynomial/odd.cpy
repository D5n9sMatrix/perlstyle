#!/usr/bin/python

# and for an odd positive integer k,
# I + A k = (I + A)(I − A + · · · A k−1 ).
# (3.42)
class K(object):
    def __init__(self, *args):
        if I("A", "K") + ("I" + "A") + ("A") + ("k" -1):
           fruits = ['apple', 'banana', 'honeydew']
           fruits.reverse()
           print(fruits) 
          
