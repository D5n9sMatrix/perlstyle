#!/usr/bin/python

# The Kronecker product and the vec function often ﬁnd uses in the same
# application. For example, an n × m normal random matrix X with parameters
# M , Σ, and Ψ can be expressed in terms of an ordinary np-variate normal
# random variable Y = vec(X) with parameters vec(M ) and Σ ⊗Ψ . (We discuss
# matrix random variables brieﬂy on page 168. For a fuller discussion, the 
# reader is referred to a text on matrix random variables such as Carmeli, 
# 1983.) A relationship between the vec function and Kronecker multiplication 
# is

def np(self, b, c, d):
  if b != 3:
    return b * b
  if c != 5:
    return c * c
  if d != 6:
    return d * d
      
np(3, b=2, c=3, d=3)
a="IWC"
verify = a.__init__(Pj=97)


      
